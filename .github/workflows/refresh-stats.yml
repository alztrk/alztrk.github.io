name: Refresh Stats

on:
  schedule:
    - cron: '0 * * * *'
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Fetch fresh stats
        run: node scripts/fetch-stats.mjs
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Build
        run: npm run build

      - name: Commit updated JSON
        run: |
          git config user.name "alztrk"
          git config user.email "alztrk@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${{ github.token }}@github.com/${{ github.repository }}
          git add src/user.json src/repos.json src/events.json src/contribs.json
          git diff --staged --quiet || git commit -m "Auto-refresh stats [skip ci]"
          git push

      - name: Deploy to gh-pages
        run: npx gh-pages -d dist -u "alztrk <alztrk@users.noreply.github.com>"
